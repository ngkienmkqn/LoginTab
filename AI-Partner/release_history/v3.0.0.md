# Login Tab v3.0.0 - Stable Windows Release

**Release Date:** 2026-01-31

## 🎯 Major Milestone
This version marks a significant milestone with stable Windows compatibility, including Windows N support. Iron Browser 141 integration provides enhanced anti-detection capabilities comparable to GemLogin.

---

## 🆕 New Features

### Iron Browser 141 Integration
- Integrated Iron Browser 141 (from GemLogin) for superior anti-detection
- Browser version: `141.0.0.0`
- User-Agent: `Chrome/141.0.0.0`
- Brands: "Chromium" + "Not)A;Brand" (shows as "Unknown browser" on iphey.com)

### Custom Profile Directory
- Added `--profile-directory` flag for custom browser profile names
- Each account now has its own named profile (like GemLogin)
- Browser shows account name instead of "Default"

### Comprehensive Automation Hiding
- Removal of `cdc_` properties (ChromeDriver detection)
- Puppeteer-specific markers cleanup
- Error stack trace filtering
- Navigator properties masking

---

## 🔧 Changes

### BrowserManager.js
- **Browser Path Priority**: GemLogin's Iron Browser 141 → Bundled → System Chrome
- **Launch Flags**: Exact GemLogin configuration for Windows N compatibility
- **Evasion Scripts**: Enhanced Level 5 stealth with automation hiding
- **Profile Directory**: Custom naming per account

### SyncManager.js
- **Session Backup Optimization**: Exclude large cache folders to reduce backup size
  - Excluded: Cache, Code Cache, GPUCache, Shader Cache, Service Worker, Blob Storage, Crashpad
  - Kept: Cookies, Local Storage, IndexedDB, Preferences (essential for auth)
- **MySQL Compatibility**: Reduced backup size prevents `max_allowed_packet` errors

### UI (index.html)
- **Modal Layout Fixed**: Corrected flexbox structure for profile modal
- **Tab Buttons**: Replaced inconsistent `tab-link` divs with proper `tab-btn` buttons
- **Footer Position**: Modal footer now correctly positioned at bottom
- **Proportions**: Reduced sidebar width (130-140px), increased modal width (750px)

---

## 🗑️ Removed

### Cleanup
- Deleted old `browsers/` folder (Chromium 134 bundle)
- Removed 50+ test/debug/temp files
- Removed screenshot artifacts
- Cleaned up unused utility scripts

---

## 🐛 Bug Fixes

### Modal UI
- Fixed Fingerprint and Extensions tabs not working (were divs instead of buttons)
- Fixed modal footer appearing inside content area
- Fixed tabs sidebar taking too much space

### Session Sync
- Fixed `max_allowed_packet` MySQL error by excluding cache folders from backup

---

## ⚠️ Known Issues

### Windows N
- `mf.dll` error appears in logs (Media Foundation not available)
- Does NOT affect functionality due to `--disable-features=AcceleratedVideoDecode` flag

### macOS
- Iron Browser not available for macOS yet
- Falls back to system Chrome/Chromium

---

## 📁 File Structure

```
LoginTab/
├── browser/
│   └── 141/
│       └── Chrome-bin/          # Iron Browser 141 (bundled)
├── src/
│   ├── managers/
│   │   ├── BrowserManager.js    # Browser launch + evasion
│   │   └── SyncManager.js       # Session backup/restore
│   └── ui/
│       └── index.html           # Fixed modal layout
└── package.json                 # v3.0.0
```

---

## 🔄 Migration Notes

### From v2.x
- Session data in "Default" folder will NOT auto-migrate to new profile directories
- Each account now has its own profile folder named after the account
- Old sessions may need to be re-created or manually migrated

---

## ✅ Tested

- **Platform**: Windows 10/11, Windows N
- **Browser**: Iron Browser 141.0.0.0
- **Anti-Detection**: iphey.com shows "Unknown browser" (intended)
- **Session Sync**: MySQL backup/restore working
- **Fingerprint**: Canvas, WebGL, Audio spoofing verified
